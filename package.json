{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "docs": "pnpm -r predoc && typedoc",
        "dev": "pnpm -r clean && pnpm run docs && pnpm -r clean && pnpm -r build"
    },
    "devDependencies": {
        "typedoc": "^0.25.4",
        "typescript": "^5.0.4"
    }
}
